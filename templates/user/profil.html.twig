{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} -Profil
{% endblock %}

{% block body %}
    <section>
        <h1>Mon profil</h1>
        <div>
            {{ form_start(user_profil) }}
                {{ form_widget(user_profil) }}

            {% if app.user.id == user.id %}
                <button>Enregistrer</button>
                <input type="reset">
            {% endif %}

            {{ form_end(user_profil) }}

        </div>
    </section>
{% endblock %}
