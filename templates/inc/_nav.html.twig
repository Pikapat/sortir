<nav>
    <section>
        <div>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('villes') }}">Villes</a>
                <a href="{{ path('campus') }}">Campus</a>
            {% endif %}
            <button><a href="{{ path('homepage') }}">Accueil</a></button>
            {% if app.user %}
                <button><a href="{{ path('sorties') }}">Sorties</a></button>
            {% endif %}

        </div>


        <div>
            {% if not is_granted('IS_AUTHENTICATED') %}
                <button><a href="{{ path('app_login') }}">Connexion</a></button>
            {% endif %}

            {% if is_granted('IS_AUTHENTICATED') %}
                <h2>Bienvenu, {{ app.user.prenom }}</h2>
            <button><a href="{{ path('profil', {'id': app.user.id}) }}">Mon profil</a></button>
            <button> <a href="{{ path('app_logout') }}">Déconnexion</a></button>
            {% endif %}

            <p>{{ "now"|date("d/m/Y", "Europe/Paris") }}</p>
        </div>
    </section>

</nav>